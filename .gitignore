# deps
node_modules/

# env: never commit. Values live in hPanel environment variables.
.env
.env.*
!.env.example
.dev.vars

# build output
dist/
.wrangler/

# parity gate output. Regenerated on every run; the GOLDEN masters under
# legacy/golden/ are committed and must never be ignored.
tests/parity-out/

# logs
logs/
*.log
npm-debug.log*

# editor / os
.vscode/*
!.vscode/launch.json
.idea/
.DS_Store

# ---------------------------------------------------------------------------
# Sandbox Cloudflare Pages scaffold. NOT part of this project: the target is
# Hostinger Node.js + MariaDB (see NCC_BUILD_SPEC.md section 2). A stray
# wrangler.jsonc would send an implementer down the wrong deployment path.
# ---------------------------------------------------------------------------
wrangler.jsonc
vite.config.ts
tsconfig.json
src/
public/
